#!/usr/bin/env ruby
require_relative "../lib/greeting.rb"

# code your CLI here!
puts "Hi! I'm HAL, what's your name?" #Welcome message outputted
name = gets.strip #Calls our local variable to capture the user's type information from terminal, removes whitespace & new lines.

greeting(name)#Calls the 'greeting' method using the 'name' variable as the argument
